# 服务器 Docker 环境完全清理指南

> **服务器**: 192.168.1.14  
> **用户**: busizeshi  
> **部署目录**: /home/busizeshi/docker  
> **操作时间**: 2026-08-18

---

## ⚠️ 严重警告

此操作会**永久删除**以下所有内容：
- ❌ 所有 Docker 容器（包括正在运行的）
- ❌ 所有 Docker 镜像
- ❌ 所有 Docker 卷（数据库数据等）
- ❌ 所有 Docker 网络
- ❌ /home/busizeshi/docker 目录及其所有内容

**数据无法恢复！请确保已备份重要数据！**

---

## 📋 执行步骤

### 方法一：使用自动化脚本（推荐）

#### 1. 上传脚本到服务器

在**本地 Windows** 上执行：

```powershell
# 使用 SCP 上传脚本
scp D:\dev\Java\FaFa\清理服务器脚本.sh busizeshi@192.168.1.14:/home/busizeshi/
```

#### 2. 连接到服务器

```powershell
ssh busizeshi@192.168.1.14
```

#### 3. 给脚本执行权限

```bash
cd /home/busizeshi
chmod +x 清理服务器脚本.sh
```

#### 4. 执行清理脚本

```bash
# 如果当前用户有 sudo 权限
sudo ./清理服务器脚本.sh

# 或者切换到 root 用户
su -
cd /home/busizeshi
./清理服务器脚本.sh
```

#### 5. 确认执行

脚本会要求输入 `YES` 进行最终确认。

---

### 方法二：手动逐步执行

如果你不想使用脚本，可以手动执行以下命令：

#### 1. 连接到服务器

```bash
ssh busizeshi@192.168.1.14
```

#### 2. 查看当前状态

```bash
# 查看所有容器
docker ps -a

# 查看所有镜像
docker images

# 查看所有卷
docker volume ls

# 查看磁盘使用
docker system df
```

#### 3. 停止所有容器

```bash
# 如果有正在运行的容器
docker stop $(docker ps -q)
```

#### 4. 删除所有容器

```bash
docker rm -f $(docker ps -aq)
```

#### 5. 删除所有镜像

```bash
docker rmi -f $(docker images -q)
```

#### 6. 删除所有卷

```bash
docker volume rm $(docker volume ls -q)
```

#### 7. 删除自定义网络

```bash
docker network prune -f
```

#### 8. 清理 Docker 系统

```bash
docker system prune -a -f --volumes
```

#### 9. 删除部署目录

```bash
# 查看目录大小
du -sh /home/busizeshi/docker

# 查看目录内容
ls -la /home/busizeshi/docker

# 删除整个目录（需要 sudo 或 root 权限）
sudo rm -rf /home/busizeshi/docker
```

#### 10. 验证清理结果

```bash
# 验证 Docker 状态
docker ps -a        # 应该为空
docker images       # 应该为空
docker volume ls    # 应该为空
docker system df    # 应该显示接近 0

# 验证目录删除
ls -la /home/busizeshi/docker  # 应该提示不存在
```

---

## 🔍 清理前的检查清单

在执行清理之前，请确认：

### 1. 备份重要数据

- [ ] 数据库数据（MySQL、PostgreSQL、MongoDB 等）
- [ ] 上传的文件（MinIO、OSS 等）
- [ ] 配置文件（docker-compose.yml、.env 等）
- [ ] 日志文件（如果需要）
- [ ] 代码和应用（虽然通常在 Git 中）

### 2. 记录关键配置

- [ ] Docker Compose 配置文件
- [ ] 环境变量配置
- [ ] 网络配置
- [ ] 端口映射
- [ ] 卷挂载路径

### 3. 确认服务状态

```bash
# 查看正在运行的服务
docker ps

# 查看服务日志（如果需要保存）
docker logs <container_name> > /tmp/backup_logs.txt
```

---

## 📁 可能需要备份的目录结构

根据你的项目，可能的目录结构：

```
/home/busizeshi/docker/
├── mysql/              # MySQL 数据目录
│   └── data/
├── redis/              # Redis 数据目录
│   └── data/
├── minio/              # MinIO 存储目录
│   └── data/
├── qdrant/             # Qdrant 向量数据库
│   └── storage/
├── nginx/              # Nginx 配置
│   └── conf/
├── fafa-java/          # Java 应用
│   └── logs/
├── fafa-python/        # Python 应用
│   └── logs/
├── web/                # 微信小程序前端
└── docker-compose.yml  # Docker Compose 配置
```

### 备份命令示例

```bash
# 创建备份目录
mkdir -p /tmp/docker-backup
cd /home/busizeshi/docker

# 备份配置文件
cp docker-compose.yml /tmp/docker-backup/
cp .env /tmp/docker-backup/ 2>/dev/null || true

# 备份数据库（示例）
docker exec mysql mysqldump -u root -p密码 数据库名 > /tmp/docker-backup/database.sql

# 打包整个目录（如果磁盘空间足够）
tar -czf /tmp/docker-backup/docker-full-$(date +%Y%m%d).tar.gz /home/busizeshi/docker
```

---

## ✅ 清理后的验证

执行清理后，验证以下内容：

### 1. Docker 状态

```bash
# 容器数量应为 0
docker ps -a | wc -l

# 镜像数量应为 0
docker images | wc -l

# 卷数量应为 0
docker volume ls | wc -l

# 磁盘使用应接近 0
docker system df
```

**预期输出**：
```
TYPE            TOTAL     ACTIVE    SIZE      RECLAIMABLE
Images          0         0         0B        0B
Containers      0         0         0B        0B
Local Volumes   0         0         0B        0B
Build Cache     0         0         0B        0B
```

### 2. 目录状态

```bash
ls -la /home/busizeshi/docker
# 预期输出: ls: cannot access '/home/busizeshi/docker': No such file or directory
```

### 3. 磁盘空间

```bash
df -h
# 检查磁盘空间是否已释放
```

---

## 🚀 清理后的重新部署

如果需要重新部署服务，请执行：

### 1. 重新创建目录结构

```bash
mkdir -p /home/busizeshi/docker/{mysql,redis,minio,nginx,logs}
```

### 2. 上传配置文件

```bash
# 从备份恢复配置文件
scp docker-compose.yml busizeshi@192.168.1.14:/home/busizeshi/docker/
```

### 3. 拉取镜像

```bash
cd /home/busizeshi/docker

# 拉取需要的镜像
docker pull mysql:8.0
docker pull redis:7.0
docker pull minio/minio
docker pull nginx:alpine
# ... 其他镜像
```

### 4. 启动服务

```bash
docker-compose up -d
```

---

## 🛠️ 常见问题

### Q1: 删除容器时提示权限不足

**解决方案**：
```bash
sudo docker rm -f $(docker ps -aq)
# 或
su - root
```

### Q2: 删除目录时提示权限不足

**解决方案**：
```bash
sudo rm -rf /home/busizeshi/docker
```

### Q3: 某些镜像删除失败

**解决方案**：
```bash
# 强制删除
docker rmi -f <image_id>

# 或删除所有未使用的镜像
docker image prune -a -f
```

### Q4: 卷正在被使用无法删除

**解决方案**：
```bash
# 先停止所有容器
docker stop $(docker ps -q)

# 再删除卷
docker volume rm $(docker volume ls -q)
```

### Q5: 想保留某些镜像

**解决方案**：
在删除镜像前，先记录需要保留的镜像名称，然后在清理后重新拉取：
```bash
# 记录镜像
docker images > /tmp/images-list.txt

# 清理后重新拉取
docker pull mysql:8.0
docker pull redis:7.0
```

---

## 📊 预期清理效果

### 清理前（示例）

```
REPOSITORY          TAG       SIZE
mysql               8.0       500MB
redis               7.0       100MB
minio/minio         latest    200MB
nginx               alpine    50MB
your-java-app       latest    300MB
your-python-app     latest    250MB

Containers: 10
Images: 15
Volumes: 8
Total Size: ~5GB
```

### 清理后

```
Containers: 0
Images: 0
Volumes: 0
Total Size: 0B

/home/busizeshi/docker: 目录不存在
```

---

## 📝 执行记录模板

建议记录清理过程：

```
清理执行记录
================

执行时间: 2026-08-18 __:__:__
执行人: busizeshi
服务器: 192.168.1.14

清理前状态:
- 容器数量: __
- 镜像数量: __
- 卷数量: __
- 目录大小: __

清理操作:
- [√] 停止所有容器
- [√] 删除所有容器
- [√] 删除所有镜像
- [√] 删除所有卷
- [√] 删除所有网络
- [√] 清理系统缓存
- [√] 删除部署目录

清理后状态:
- 容器数量: 0
- 镜像数量: 0
- 卷数量: 0
- 目录状态: 已删除

完成时间: 2026-08-18 __:__:__
```

---

## 🎯 总结

### 推荐执行流程

1. ✅ **备份重要数据**（数据库、文件、配置）
2. ✅ **记录现有配置**（docker-compose.yml 等）
3. ✅ **上传清理脚本**到服务器
4. ✅ **执行清理脚本**（`sudo ./清理服务器脚本.sh`）
5. ✅ **验证清理结果**（检查容器、镜像、目录）
6. ✅ **记录清理过程**（时间、结果等）

### 关键命令速查

```bash
# 一键清理（慎用！）
docker stop $(docker ps -q) && \
docker rm -f $(docker ps -aq) && \
docker rmi -f $(docker images -q) && \
docker volume rm $(docker volume ls -q) && \
docker system prune -a -f --volumes && \
sudo rm -rf /home/busizeshi/docker
```

---

**文档创建时间**: 2026-08-18  
**脚本位置**: `D:\dev\Java\FaFa\清理服务器脚本.sh`  
**操作状态**: ⏳ 等待执行

---

## ⚠️ 最后提醒

**再次确认：此操作不可逆！所有数据将永久丢失！**

如果确认要执行，请：
1. 连接到服务器：`ssh busizeshi@192.168.1.14`
2. 上传并执行脚本，或手动执行上述命令
3. 按提示输入 `YES` 确认

**祝你操作顺利！** 🚀
