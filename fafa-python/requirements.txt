# FaFa AI 服务依赖
# Python 3.11+；Windows 开发环境不安装 RocketMQ 客户端（仅 Linux 消费者需要）

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4
httpx>=0.27
loguru>=0.7

# LangChain 生态（模型调用走百炼 OpenAI 兼容接口）
langchain>=0.3
langchain-openai>=0.2
langgraph>=0.2

# 向量数据库
qdrant-client>=1.11

# 阿里云百炼（多模态嵌入 qwen3-vl-embedding 需原生 SDK，OpenAI 兼容接口不覆盖该能力）
dashscope>=1.20

# 仅 Linux 部署环境安装（MQ 消费者）：
# pip install rocketmq-client-python
