<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fafa.infrastructure.persistence.mapper.UserTagMapper">

    <select id="selectByUserId" resultType="com.fafa.infrastructure.persistence.dataobject.UserTagDO">
        SELECT *
        FROM user_tag
        WHERE user_id = #{userId}
        AND is_deleted = 0
        ORDER BY usage_count DESC, created_at DESC
    </select>

    <select id="selectByUserIdOrderByUsage" resultType="com.fafa.infrastructure.persistence.dataobject.UserTagDO">
        SELECT *
        FROM user_tag
        WHERE user_id = #{userId}
        AND is_deleted = 0
        ORDER BY usage_count DESC, last_used_at DESC
        LIMIT #{limit}
    </select>

    <select id="selectByUserIdAndTagName" resultType="com.fafa.infrastructure.persistence.dataobject.UserTagDO">
        SELECT *
        FROM user_tag
        WHERE user_id = #{userId}
        AND tag_name = #{tagName}
        AND is_deleted = 0
        LIMIT 1
    </select>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*)
        FROM user_tag
        WHERE user_id = #{userId}
        AND is_deleted = 0
    </select>

</mapper>
