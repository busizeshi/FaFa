# 开发环境配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fafa?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root

  data:
    redis:
      host: localhost
      port: 6379
      password:

# 日志级别
logging:
  level:
    root: INFO
    com.fafa: DEBUG
    com.baomidou.mybatisplus: DEBUG

# RocketMQ
rocketmq:
  name-server: localhost:9876

# MinIO
minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin

# AI 服务
fafa:
  ai-service:
    base-url: http://localhost:8000
