# 技术架构与实现方案（个人开发版）

## 一、整体架构

### 1.1 架构设计原则

- **简单实用**：单体架构，双服务模式（Java + Python）
- **职责分离**：Java 处理业务逻辑，Python 专注 AI 能力
- **部署简单**：应用 Docker 化部署，中间件复用服务器已部署实例
- **成本可控**：语言模型统一使用 qwen3.7-flash，性价比高

### 1.2 系统架构图

```
┌─────────────────────────────────────────────┐
│            微信小程序端                        │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐     │
│  │  首页   │  │  记录   │  │  宠物   │     │
│  └─────────┘  └─────────┘  └─────────┘     │
│  ┌─────────┐  ┌─────────┐                  │
│  │  AI助手 │  │  我的   │                  │
│  └─────────┘  └─────────┘                  │
└─────────────────┬───────────────────────────┘
                  │ HTTPS
                  ↓
┌─────────────────────────────────────────────┐
│           Nginx (反向代理)                   │
└─────────────────┬───────────────────────────┘
                  │
    ┌─────────────┴─────────────┐
    ↓                           ↓
┌────────────┐            ┌──────────────┐
│ Java 服务   │            │ Python 服务   │
│            │            │              │
│ Spring Boot│◄──RocketMQ─┤ FastAPI      │
│            │            │              │
│ - 用户管理  │            │ - LangChain  │
│ - 宠物档案  │            │ - LangGraph  │
│ - 日常记录  │            │ - AI Agent   │
│ - 提醒系统  │            │ - 图片理解   │
│ - 报告查询  │            │ - 向量搜索   │
└─────┬──────┘            └───────┬──────┘
      │                           │
      └────────────┬──────────────┘
                   ↓
    ┌──────────────────────────────┐
    │    中间件层（已部署在服务器）   │
    │                               │
    │ ┌────────┐    ┌────────┐    │
    │ │ MySQL  │    │ Redis  │    │
    │ └────────┘    └────────┘    │
    │ ┌────────┐    ┌────────┐    │
    │ │ Qdrant │    │RocketMQ│    │
    │ │ 向量库  │    │ 消息队列│   │
    │ └────────┘    └────────┘    │
    │ ┌────────────────────────┐  │
    │ │ MinIO / 阿里云 OSS      │  │
    │ │     对象存储             │  │
    │ └────────────────────────┘  │
    └──────────────────────────────┘
```

## 二、技术栈选型

### 2.1 前端（小程序）

**框架**
- **uni-app** (Vue3)
  - 跨平台能力（未来可扩展）
  - 完善的生态和文档
  - 组件库：uView UI

**状态管理**
- **Pinia**

**请求库**
- **uni.request** 封装
- 支持 Token 自动刷新

### 2.2 Java 服务

**核心框架**
- **Spring Boot 3.x**
- **JDK 21**（可配合 Spring Boot 3 开启虚拟线程，提升 IO 密集场景吞吐）

**Web 框架**
- **Spring Web** (RESTful API)

**数据访问**
- **MyBatis-Plus** 
  - 简化 CRUD
  - 分页插件
  - 自动填充

**安全认证**
- **Sa-Token**（轻量级鉴权框架）
  - 依赖：`sa-token-spring-boot3-starter` + `sa-token-redis-jackson`（Token 存 Redis）
  - 微信登录集成（code 换 openid 后 `StpUtil.login()`）
  - 账密登录支持
  - 注解鉴权：`@SaCheckLogin` / `@SaCheckRole`

**缓存**
- **Redisson**（`redisson-spring-boot-starter`）
  - 分布式缓存（搭配 Spring Cache 注解使用）
  - 分布式锁（提醒任务防重复调度等场景）
  - 复用 `spring.data.redis` 配置，与 Sa-Token 共用 Redis

**消息队列**
- **RocketMQ Spring Boot Starter**

**对象存储**
- **MinIO SDK** (本地开发)
- **Aliyun OSS SDK** (生产环境)

**工具库**
- **Hutool** (工具类)
- **Lombok** (代码简化)
- **MapStruct** (对象映射)

### 2.3 Python 服务

**Web 框架**
- **FastAPI**
  - 高性能
  - 自动 API 文档
  - 类型检查

**AI 框架**
- **LangChain**
  - LLM 集成
  - 提示词模板
  - 输出解析

- **LangGraph**
  - Agent 工作流
  - 状态管理
  - 复杂决策链

**模型调用**
- **阿里云 DashScope SDK**
  - qwen3.7-flash (统一语言模型：日常对话 + 报告生成，速度快、成本低)
  - qwen-vl-plus (多模态，图片理解)
  - text-embedding-v2 (向量化)

**向量数据库**
- **Qdrant Client**

**消息队列**
- **rocketmq-client-python**

**工具库**
- **Pydantic** (数据验证)
- **httpx** (异步 HTTP)
- **Pillow** (图片处理)

### 2.4 中间件

> **部署说明**：以下中间件均已部署在服务器上，各应用（Java / Python / 本地开发）通过服务器地址访问，无需重复搭建；服务器防火墙/安全组只放行必要端口。

**数据库**
- **MySQL 8.0+**
  - InnoDB 存储引擎
  - JSON 字段支持

**缓存**
- **Redis 7.0+**
  - String / Hash / List / Set
  - 过期策略

**向量数据库**
- **Qdrant 1.7+**
  - 开源免费
  - 高性能向量搜索
  - Docker 部署简单

**消息队列**
- **RocketMQ 5.x**
  - 异步处理（照片 AI 分析、报告生成）
  - 削峰填谷

**对象存储**
- **MinIO** (本地开发)
- **阿里云 OSS** (生产环境)

### 2.5 日志与监控

**日志（不引入 ELK）**
- **Java 服务**：Spring Boot 默认 Logback，同时输出控制台与 `logs/` 目录，按天 + 大小滚动切割
- **Python 服务**：`logging` + `RotatingFileHandler`，输出到 `logs/` 目录
- 排查问题直接在服务器上 `tail -f` 查看日志文件即可

**基础监控**
- **Spring Boot Actuator**：`/actuator/health` 健康检查
- 中间件状态在服务器上直接查看（`docker ps` / `systemctl status` 等）

## 三、服务详细设计

### 3.1 Java 服务（业务服务）

**端口**：8080

**模块划分**

```
src/main/java/com/fafa/pet/
├── common/              # 公共模块
│   ├── config/         # 配置类
│   ├── constant/       # 常量
│   ├── exception/      # 异常处理
│   └── utils/          # 工具类
├── module/
│   ├── user/           # 用户模块
│   │   ├── controller/
│   │   ├── service/
│   │   ├── mapper/
│   │   └── entity/
│   ├── pet/            # 宠物档案模块
│   ├── record/         # 日常记录模块
│   │   ├── feeding/    # 喂食
│   │   ├── weight/     # 体重
│   │   ├── water/      # 饮水
│   │   └── event/      # 事件
│   ├── reminder/       # 提醒系统
│   ├── photo/          # 照片管理
│   ├── diary/          # 日记
│   ├── food/           # 食品管理
│   ├── report/         # 报告查询
│   └── ai/             # AI 接口（调用 Python 服务）
└── PetApplication.java
```

**核心功能**

1. **用户认证**
   - 微信登录（code 换 openid）
   - 账密登录（可选）
   - Sa-Token 管理 Token（存储于 Redis，支持踢人下线、多端登录等）

2. **宠物档案 CRUD**
   - 创建、编辑、删除宠物
   - 宠物主页数据聚合

3. **日常记录**
   - 喂食、体重、饮水、排便记录
   - 通用事件记录

4. **照片管理**
   - 上传到 MinIO/OSS
   - 发送消息到 RocketMQ 触发 AI 分析

5. **提醒系统**
   - 提醒 CRUD
   - 定时任务扫描（@Scheduled）
   - 推送小程序订阅消息

6. **报告查询**
   - 查询已生成的报告
   - 触发报告生成（发送 MQ 消息）

7. **AI 代理接口**
   - 调用 Python 服务的 AI 能力
   - WebSocket 实时对话（可选）

**配置示例 (application.yml)**

```yaml
spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:server-ip}:${MYSQL_PORT:3306}/fafa_pet?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: ${MYSQL_USERNAME:root}
    password: ${MYSQL_PASSWORD:your_password}
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  data:
    redis:
      host: ${REDIS_HOST:server-ip}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      database: 0

  # Java 21 虚拟线程（可选）
  threads:
    virtual:
      enabled: true
  

# RocketMQ（部署在服务器）
rocketmq:
  name-server: ${ROCKETMQ_NAME_SERVER:server-ip:9876}
  producer:
    group: fafa-producer-group

# Sa-Token（轻量级鉴权）
sa-token:
  token-name: satoken
  timeout: 2592000          # Token 有效期（秒），30 天
  active-timeout: -1        # 不做活跃检查
  is-concurrent: true       # 允许多端同时登录
  token-style: uuid
  is-log: false

# 对象存储配置
storage:
  type: ${STORAGE_TYPE:minio}  # minio 或 oss
  minio:
    endpoint: ${MINIO_ENDPOINT:http://server-ip:9000}
    access-key: minioadmin
    secret-key: minioadmin
    bucket: fafa-pet
  oss:
    endpoint: oss-cn-hangzhou.aliyuncs.com
    access-key-id: ${OSS_ACCESS_KEY_ID}
    access-key-secret: ${OSS_ACCESS_KEY_SECRET}
    bucket: fafa-pet

# 微信小程序配置
wechat:
  mini:
    app-id: ${WECHAT_APP_ID}
    app-secret: ${WECHAT_APP_SECRET}

# Python 服务地址
ai:
  service:
    url: ${AI_SERVICE_URL:http://localhost:8000}
```

> 说明：`server-ip` 替换为服务器实际地址；Redisson Starter 直接复用 `spring.data.redis` 配置，无需单独配置；敏感信息生产环境通过环境变量注入。

### 3.2 Python 服务（AI 服务）

**端口**：8000

**目录结构**

```
src/
├── api/                # API 路由
│   ├── chat.py        # AI 对话
│   ├── photo.py       # 图片理解
│   ├── report.py      # 报告生成
│   └── search.py      # 向量搜索
├── agent/             # LangGraph Agent
│   ├── tools.py       # Agent 工具定义
│   ├── graph.py       # Agent 工作流
│   └── state.py       # 状态管理
├── service/
│   ├── llm.py         # LLM 调用封装
│   ├── embedding.py   # 向量化服务
│   ├── qdrant.py      # Qdrant 操作
│   └── mysql.py       # MySQL 查询（通过 Java API 或直接查询）
├── consumer/          # RocketMQ 消费者
│   ├── photo_analysis.py  # 照片分析消费者
│   └── report_gen.py      # 报告生成消费者
├── config/
│   └── settings.py    # 配置管理
└── main.py            # FastAPI 入口
```

**核心功能**

1. **AI 对话 Agent**
   - 接收用户问题
   - 使用 LangGraph 构建工作流
   - 调用工具获取宠物数据
   - 返回自然语言回答

2. **照片理解（消费 MQ 消息）**
   - 从 MQ 接收照片 URL
   - 调用 qwen-vl-plus 理解照片
   - 提取场景、行为、物品
   - 生成文本描述并向量化
   - 存储到 Qdrant

3. **向量搜索**
   - 接收搜索 query
   - query 向量化
   - Qdrant 检索
   - 返回相关照片

4. **报告生成（消费 MQ 消息）**
   - 从 MQ 接收报告生成请求
   - 查询时间范围内的所有数据
   - 统计分析
   - 使用 qwen3.7-flash 生成总结
   - 存储报告到 MySQL

**配置示例 (settings.py)**

```python
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    # 阿里云 DashScope
    dashscope_api_key: str
    
    # 中间件均已部署在服务器，host 填服务器实际地址（建议环境变量注入）
    # Qdrant
    qdrant_host: str = "server-ip"
    qdrant_port: int = 6333
    
    # MySQL
    mysql_host: str = "server-ip"
    mysql_port: int = 3306
    mysql_user: str = "root"
    mysql_password: str
    mysql_database: str = "fafa_pet"
    
    # Redis
    redis_host: str = "server-ip"
    redis_port: int = 6379
    
    # RocketMQ
    rocketmq_name_server: str = "server-ip:9876"
    rocketmq_group: str = "fafa-consumer-group"
    
    # Java 服务地址
    java_service_url: str = "http://localhost:8080"
    
    class Config:
        env_file = ".env"

settings = Settings()
```

### 3.3 服务间通信

**Java → Python（同步调用）**

```java
// Java 调用 Python AI 服务
@Service
public class AiService {
    @Value("${ai.service.url}")
    private String aiServiceUrl;
    
    private final RestTemplate restTemplate;
    
    public String chat(String userId, String petId, String message) {
        ChatRequest request = new ChatRequest(userId, petId, message);
        ChatResponse response = restTemplate.postForObject(
            aiServiceUrl + "/api/chat",
            request,
            ChatResponse.class
        );
        return response.getAnswer();
    }
}
```

**Java → Python（异步 MQ）**

```java
// Java 发送照片分析消息
@Service
public class PhotoService {
    @Autowired
    private RocketMQTemplate rocketMQTemplate;
    
    public void uploadPhoto(MultipartFile file, Long petId) {
        // 1. 上传到 OSS
        String url = storageService.upload(file);
        
        // 2. 保存到数据库
        Photo photo = photoMapper.insert(new Photo(petId, url));
        
        // 3. 发送 MQ 消息触发 AI 分析
        PhotoAnalysisMessage message = new PhotoAnalysisMessage(
            photo.getId(), 
            photo.getPetId(), 
            url
        );
        rocketMQTemplate.convertAndSend("photo-analysis", message);
    }
}
```

```python
# Python 消费照片分析消息
@consumer.subscribe("photo-analysis")
async def handle_photo_analysis(message: dict):
    photo_id = message['photoId']
    pet_id = message['petId']
    url = message['url']
    
    # 调用多模态模型理解照片
    result = await vision_service.analyze(url)
    # {
    #   "scene": "阳台",
    #   "behavior": "睡觉",
    #   "objects": ["蓝色猫窝"],
    #   "description": "豆包趴在阳台蓝色猫窝里睡觉"
    # }
    
    # 向量化描述
    embedding = await embedding_service.embed(result['description'])
    
    # 存储到 Qdrant
    await qdrant_service.insert(
        collection_name="pet_photos",
        point_id=photo_id,
        vector=embedding,
        payload={
            "pet_id": pet_id,
            "description": result['description'],
            "tags": result['objects']
        }
    )
    
    # 更新 MySQL
    await mysql_service.update_photo(photo_id, result)
```

## 四、LangGraph Agent 设计

### 4.1 Agent 工具定义

```python
from langchain.tools import tool
from typing import List, Dict
import httpx

@tool
async def get_pet_profile(pet_id: str) -> Dict:
    """获取宠物基础档案信息"""
    async with httpx.AsyncClient() as client:
        response = await client.get(
            f"{settings.java_service_url}/api/pets/{pet_id}"
        )
        return response.json()

@tool
async def get_weight_history(pet_id: str, days: int = 30) -> List[Dict]:
    """获取宠物体重历史记录"""
    async with httpx.AsyncClient() as client:
        response = await client.get(
            f"{settings.java_service_url}/api/records/weight",
            params={"petId": pet_id, "days": days}
        )
        return response.json()

@tool
async def get_feeding_history(pet_id: str, days: int = 30) -> List[Dict]:
    """获取宠物喂食历史记录"""
    async with httpx.AsyncClient() as client:
        response = await client.get(
            f"{settings.java_service_url}/api/records/feeding",
            params={"petId": pet_id, "days": days}
        )
        return response.json()

@tool
async def search_photos(pet_id: str, query: str, limit: int = 10) -> List[Dict]:
    """语义搜索宠物照片"""
    # 向量化 query
    query_embedding = await embedding_service.embed(query)
    
    # Qdrant 搜索
    results = await qdrant_client.search(
        collection_name="pet_photos",
        query_vector=query_embedding,
        query_filter={
            "must": [{"key": "pet_id", "match": {"value": pet_id}}]
        },
        limit=limit
    )
    
    return [
        {
            "photo_id": r.id,
            "score": r.score,
            "description": r.payload['description'],
            "url": r.payload.get('url')
        }
        for r in results
    ]
```

### 4.2 Agent 工作流（LangGraph）

```python
from langgraph.graph import StateGraph, END
from langchain_community.chat_models import ChatTongyi
from typing import TypedDict, Annotated
import operator

class AgentState(TypedDict):
    messages: Annotated[list, operator.add]
    pet_id: str
    data: dict

# 初始化 LLM
llm = ChatTongyi(
    model="qwen3.7-flash",
    dashscope_api_key=settings.dashscope_api_key
)

# 绑定工具
llm_with_tools = llm.bind_tools([
    get_pet_profile,
    get_weight_history,
    get_feeding_history,
    search_photos
])

def call_model(state: AgentState):
    """调用 LLM"""
    response = llm_with_tools.invoke(state['messages'])
    return {"messages": [response]}

def should_continue(state: AgentState):
    """判断是否需要调用工具"""
    last_message = state['messages'][-1]
    if last_message.tool_calls:
        return "tools"
    return END

def call_tools(state: AgentState):
    """执行工具调用"""
    last_message = state['messages'][-1]
    results = []
    
    for tool_call in last_message.tool_calls:
        tool_name = tool_call['name']
        tool_args = tool_call['args']
        
        # 注入 pet_id
        if 'pet_id' in tool_args and not tool_args['pet_id']:
            tool_args['pet_id'] = state['pet_id']
        
        # 执行工具
        tool_result = await globals()[tool_name].ainvoke(tool_args)
        results.append({
            "tool_call_id": tool_call['id'],
            "output": tool_result
        })
    
    return {"messages": results}

# 构建图
workflow = StateGraph(AgentState)
workflow.add_node("agent", call_model)
workflow.add_node("tools", call_tools)
workflow.set_entry_point("agent")
workflow.add_conditional_edges("agent", should_continue, {
    "tools": "tools",
    END: END
})
workflow.add_edge("tools", "agent")

agent = workflow.compile()
```

### 4.3 API 接口

```python
from fastapi import FastAPI, WebSocket
from pydantic import BaseModel

app = FastAPI()

class ChatRequest(BaseModel):
    user_id: str
    pet_id: str
    message: str
    session_id: str = None

@app.post("/api/chat")
async def chat(request: ChatRequest):
    """AI 对话接口"""
    # 获取历史对话
    history = await get_conversation_history(request.session_id)
    
    # 构建状态
    state = {
        "messages": history + [("human", request.message)],
        "pet_id": request.pet_id,
        "data": {}
    }
    
    # 执行 Agent
    result = await agent.ainvoke(state)
    
    # 获取最终回答
    answer = result['messages'][-1].content
    
    # 保存对话历史
    await save_conversation(request.session_id, request.message, answer)
    
    return {"answer": answer}

@app.websocket("/ws/chat")
async def chat_websocket(websocket: WebSocket):
    """WebSocket 实时对话（流式输出）"""
    await websocket.accept()
    
    while True:
        data = await websocket.receive_json()
        
        async for chunk in agent.astream(data):
            await websocket.send_json(chunk)
```

## 五、部署方案

### 5.1 Docker Compose 配置

> 中间件（MySQL、Redis、Qdrant、RocketMQ、MinIO）均已部署在服务器上，Compose 只编排应用服务（Java、Python、Nginx）；日志目录挂载到宿主机 `./logs/` 方便直接查看。

```yaml
version: '3.8'

services:
  # Java 服务
  java-service:
    build:
      context: ./java-service
      dockerfile: Dockerfile
    container_name: fafa-java-service
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: ${ENV:-prod}
      # 中间件地址（服务器上已部署）
      MYSQL_HOST: ${SERVER_HOST}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      REDIS_HOST: ${SERVER_HOST}
      REDIS_PASSWORD: ${REDIS_PASSWORD:-}
      ROCKETMQ_NAME_SERVER: ${SERVER_HOST}:9876
      STORAGE_TYPE: ${STORAGE_TYPE:-minio}
      MINIO_ENDPOINT: http://${SERVER_HOST}:9000
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY}
    volumes:
      - ./logs/java-service:/app/logs
    networks:
      - fafa-network

  # Python 服务
  python-service:
    build:
      context: ./python-service
      dockerfile: Dockerfile
    container_name: fafa-python-service
    ports:
      - "8000:8000"
    environment:
      DASHSCOPE_API_KEY: ${DASHSCOPE_API_KEY}
      # 中间件地址（服务器上已部署）
      MYSQL_HOST: ${SERVER_HOST}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      REDIS_HOST: ${SERVER_HOST}
      QDRANT_HOST: ${SERVER_HOST}
      ROCKETMQ_NAME_SERVER: ${SERVER_HOST}:9876
      JAVA_SERVICE_URL: http://java-service:8080
    volumes:
      - ./logs/python-service:/app/logs
    networks:
      - fafa-network

  # Nginx
  nginx:
    image: nginx:alpine
    container_name: fafa-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
    depends_on:
      - java-service
      - python-service
    networks:
      - fafa-network

networks:
  fafa-network:
    driver: bridge
```

### 5.2 Java 服务 Dockerfile

```dockerfile
FROM eclipse-temurin:21-jre

WORKDIR /app

COPY target/*.jar app.jar

EXPOSE 8080

ENTRYPOINT ["java", "-jar", "-Xmx512m", "-Xms256m", "app.jar"]
```

### 5.3 Python 服务 Dockerfile

```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/

COPY . .

EXPOSE 8000

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

### 5.4 启动命令

```bash
# 启动所有服务
docker-compose up -d

# 查看日志
docker-compose logs -f java-service
docker-compose logs -f python-service

# 停止服务
docker-compose down

# 重新构建
docker-compose up -d --build
```

## 六、日志方案

不引入 ELK，日志直接输出到文件（控制台 + 文件双写，按天 + 大小滚动切割），排查问题时 `tail -f` 查看即可。

### 6.1 Java 服务（Logback）

`logback-spring.xml`：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 文件输出：按天 + 大小滚动 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/java-service.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/java-service.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>2GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>
```

### 6.2 Python 服务

```python
import logging
from logging.handlers import RotatingFileHandler

handler = RotatingFileHandler(
    "logs/python-service.log",
    maxBytes=50 * 1024 * 1024,  # 50MB
    backupCount=10,
    encoding="utf-8"
)
logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] %(name)s - %(message)s",
    handlers=[handler]
)
```

### 6.3 健康检查与日志查看

- 健康检查：`GET /actuator/health`（Spring Boot Actuator）
- 中间件状态：在服务器上 `docker ps` / `systemctl status xxx` 查看

```bash
# 查看应用日志（Compose 已将容器内 logs 目录挂载到宿主机 ./logs/）
tail -f logs/java-service/java-service.log
tail -f logs/python-service/python-service.log

# 或直接查看容器日志
docker logs -f fafa-java-service
```

## 七、开发建议

### 7.1 本地开发环境

中间件已部署在服务器上，本地开发无需搭建，直接连接服务器即可；Java 和 Python 服务在 IDE 中运行，方便调试。

**方式一：直连服务器（简单）**
- 本地配置（application.yml / .env）中中间件地址填服务器 IP
- 服务器安全组仅对开发机 IP 放行所需端口

**方式二：SSH 隧道转发（更安全，推荐）**
```bash
# 把服务器中间件端口转发到本地，本地配置仍用 localhost
ssh -N -L 3306:127.0.0.1:3306 \
       -L 6379:127.0.0.1:6379 \
       -L 6333:127.0.0.1:6333 \
       -L 9876:127.0.0.1:9876 \
       -L 9000:127.0.0.1:9000 \
       user@server-ip
```

### 7.2 开发流程

1. **Java 服务开发**
   - IDEA / Eclipse
   - Spring Boot DevTools 热重载
   - 单元测试 + 集成测试

2. **Python 服务开发**
   - PyCharm / VSCode
   - uvicorn --reload 热重载
   - pytest 测试

3. **小程序开发**
   - 微信开发者工具
   - HBuilderX (uni-app)

### 7.3 代码规范

**Java**
- 阿里巴巴 Java 开发规约
- CheckStyle / SonarLint

**Python**
- PEP 8
- Black (代码格式化)
- Ruff (Linter)

## 八、成本估算

### 8.1 阿里云 AI 成本

> 以下为估算，具体以阿里云官方刊例价为准。

**qwen3.7-flash**（日常对话 + 报告生成）
- flash 系列单价极低（约 ¥0.0005 / 1K tokens 量级）
- 假设日均 1000 次对话（约 500 tokens）+ 月均 1000 份报告（约 2K tokens）
- 月成本：约 ¥10 以内

**qwen-vl-plus**（图片理解）
- ¥0.018 / 1K tokens（图片 + 文本）
- 假设日均 500 张照片
- 月成本：500 × 30 × 0.018 ≈ ¥270

**text-embedding-v2**（向量化）
- ¥0.0007 / 1K tokens
- 假设日均 500 次向量化，平均 100 tokens
- 月成本：约 ¥1

**总计：约 ¥280/月**（1000 日活用户，主要开销在图片理解）

### 8.2 服务器成本

**服务器**
- 已有服务器（中间件均已部署），无新增成本

**对象存储**
- 使用服务器自建 MinIO：¥0
- 如使用阿里云 OSS：存储 ¥0.12/GB/月，流量 ¥0.5/GB

**总计：¥0 ~ ¥100/月**（视对象存储方案而定）

### 8.3 总成本

**初期（1000 日活）**
- AI 成本：约 ¥280/月
- 服务器：¥0（复用已有服务器，中间件已部署）
- **总计：约 ¥300/月**

## 九、总结

这套架构方案：

✅ **简单实用**：双服务架构，职责清晰  
✅ **技术栈轻量**：Java 21 + Spring Boot 3.x + Sa-Token + Redisson  
✅ **易于开发**：Java + Python 各司其职  
✅ **成本可控**：语言模型统一使用 qwen3.7-flash  
✅ **部署简单**：中间件复用服务器已有部署，Compose 只编排应用  
✅ **日志轻量**：直接输出到文件，无需 ELK  

非常适合个人开发者快速启动和迭代！
