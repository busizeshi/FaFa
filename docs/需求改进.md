# com.fafa.interfaces.controller.PhotoController#uploadPhoto

- 需要增加一个参数，tags，用来存储用户对照片或视频的标签，标签之间用逗号隔开，用List<String>存储。
- 我希望可以选择批量上传。
- 去掉宠物id吧，我们可以使用tags来直接打标签每个照片来标识每个宠物。Java和python都要去掉
- 另外我们需要有一个表来存储不同用户的tags，这样不会让用户每次都新建tags。所有我们需要增删改查，批量删，批量查询。
